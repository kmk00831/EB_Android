###############################################################################
# 1) 기본 규칙 – 모든 텍스트는 LF 줄바꿈으로 저장
* text=auto eol=lf

###############################################################################
# 2) 주 언어 & 스크립트 – 명시적으로 LF 강제
*.kt        text eol=lf
*.java      text eol=lf
*.gradle    text eol=lf
*.groovy    text eol=lf
*.xml       text eol=lf
*.properties text eol=lf
*.sh        text eol=lf
*.md        text eol=lf

###############################################################################
# 3) 바이너리 파일 – diff/충돌 방지 (줄바꿈 변환 OFF)
*.png       binary
*.jpg       binary
*.jpeg      binary
*.gif       binary
*.webp      binary
*.jar       binary
*.aar       binary
*.keystore  binary

###############################################################################
# 4) (선택) Git LFS로 관리할 대용량 산출물
#    LFS를 이미 설정했거나 향후 APK/AAB를 커밋할 계획이면 유지,
#    그렇지 않다면 아래 두 줄을 삭제해도 됩니다.
*.apk       filter=lfs diff=lfs merge=lfs -text
*.aab       filter=lfs diff=lfs merge=lfs -text
